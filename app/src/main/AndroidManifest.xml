<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.arkton.android.smarthomemobile">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartHomeMobile">
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.KitchenActivity"
            android:exported="false"
            android:label="@string/labelTitleKitchen"
            android:parentActivityName=".activities.KitchenActivity" />
        <activity
            android:name=".activities.OutsideActivity"
            android:exported="false"
            android:label="@string/labelTitleOutside"
            android:parentActivityName=".activities.OutsideActivity" />
        <activity
            android:name=".activities.GuestActivity"
            android:exported="false"
            android:label="@string/labelTitleGuest"
            android:parentActivityName=".activities.MainActivity" />
    </application>

</manifest>